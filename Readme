# To run:

make
make install

# Inside postgres(psql):

CREATE EXTENSION pg_gpt;
SELECT pg_gpt();

# Path of files in postgresql

/usr/share/postgresql/15/extension/pg_gpt--0.0.1.sql
/usr/share/postgresql/15/extension/pg_gpt_sum.control
